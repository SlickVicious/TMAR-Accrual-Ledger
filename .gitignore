node_modules/
.DS_Store
coverage/

# NOI Agent Browser — Do Not Upload
bearer.token
config.toml
gateway.json
logs/
memory/
sessions/

# Local build artifacts & reference scans
.tmar-build/
agent_scan_FULL.md
agent_scan_pages_preview.txt

# Data exports (local only)
*.csv
*.xlsx

# Claude Code project settings (local dev environment, not repo config)
# Ignore everything under .claude/ by default...
.claude/*
# ...but SHARE team-usable subagents + skills so other machines (Mac) can pull them.
!.claude/agents/
!.claude/skills/
# Security-sensitive / machine-local files stay ignored on purpose:
#   .claude/settings.local.json  — permission allowlist + local machine paths
#   .claude/hooks/               — local guardrail scripts (secret-blocker, test-runner)

# Playwright MCP session snapshots (ephemeral browser tool artifacts)
.playwright-mcp/

# Runtime-generated HTML backups (vault backup, not source)
*.bak-vault

# Debug logs
firebase-debug.log

# Audit/test session artifacts
screenshot-*.png
screenshots/
tt-screenshots/
tt-page-audit.cjs
tt-test-output.md
tt-test-runner.py
tt-test.html

.env*
.flaskenv*
!.env.project
scripts/.tmar-gas-key

# Local IDE / editor settings
.vscode/

# OpenAI Codex workspace config (local dev environment)
.codex/

# Merge artifacts and pre-dedup backups
TMAR-Accrual-Ledger-merged.html
*.bak-predeup

# Local debug screenshots
eon-*.png

# Local GAS setup helpers (not deployed)
gas/env-vault-setup/

# IRS reference PDFs (static official forms, not generated artifacts)
irs-forms/downloads/

# fiduciary-doc-factory source-book PDFs (large/copyrighted reference manuals — kept local,
# re-ignored under the .claude/skills/ whitelist above)
.claude/skills/fiduciary-doc-factory/references/source-books/

# Local build/dev tooling — kept on disk, out of source control.
# Bank-statement parsers, FileCabinet build scripts, one-off injectors, cleanup.
# NOTE: scripts/filecabinet-build/ holds 4 real GAS files worth promoting into
# gas/ later if formalized: CapitalOneCashFlow.gs, appc_import_delta.gs,
# appc_import_full_replace.gs, path_remap.gs.
scripts/
# ...but skill scripts under the tracked .claude/skills/ whitelist are part of the skill.
!.claude/skills/**/scripts/
!.claude/skills/**/scripts/**
tmar_cleanup.ps1

# Hermes Agent vault context — share the vault identity file, ignore runtime state
.hermes/runtime/*
!.hermes/
!.hermes/vault-context.md

# Obsidian — ignore everything by default (local editor state)...
.obsidian/*
# ...but SHARE the vault standard configs so every clone has the same theme,
# plugins, and homepage. Workspace state (workspace.json, hotkeys.json, etc.)
# stays local.
!.obsidian/app.json
!.obsidian/appearance.json
!.obsidian/community-plugins.json
!.obsidian/core-plugins.json
!.obsidian/core-plugins-migration.json
!.obsidian/homepage.json
# hermes/ subdir inside .obsidian/ (context bridge) — ignore runtime state, share config
!.obsidian/hermes/
!.obsidian/hermes/context.json
.obsidian/hermes/runtime/*
